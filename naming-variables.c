#include <stdio.h>

int main(void){
    int number;//i put out the excalmation sign cinform by the rules.
int test_value;//i change the space between test and value with an underscore conform to the rules..
int example1;//I change the numper one before example to after od the word example because we can't start a word witha number.
float int_;// I put a underscore like int_ for that word left be a key word.
printf("Variables created successfully.\n");
}
